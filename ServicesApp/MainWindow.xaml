<Window
    x:Class="ServicesApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:ServicesApp"
    xmlns:tb="using:H.NotifyIcon"
    xmlns:models="using:Services.Core.Models"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.Resources>
            <local:StatusColorConverter x:Key="StatusColorConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Height="40" Grid.Row="0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="0" Padding="16,0,0,0" VerticalAlignment="Center" Spacing="12">
                <TextBlock Text="ServicesApp" Style="{StaticResource CaptionTextBlockStyle}" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Padding="16,0,16,8">
            <Grid Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8">
                <CommandBar DefaultLabelPosition="Right" Background="Transparent" IsOpen="False">
                    <AppBarButton Icon="Add" Label="添加服务" Click="OnAddServiceClick" ToolTipService.ToolTip="创建一个新服务"/>
                    <AppBarSeparator/>
                    <AppBarButton Icon="Refresh" Label="刷新" Click="OnRefreshClick" ToolTipService.ToolTip="刷新服务列表"/>
                    <AppBarSeparator/>
                    <AppBarButton Label="环境变量" Click="OnEnvVarsClick" ToolTipService.ToolTip="管理系统环境变量">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE81E;"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Icon="Setting" Label="设置" Click="OnSettingsClick"/>
                </CommandBar>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Padding="16,0,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Padding="16,0,16,8" Margin="0,0,12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="服务名称" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                <TextBlock Grid.Column="1" Text="状态" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                <TextBlock Grid.Column="2" Text="程序路径" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                <TextBlock Grid.Column="3" Text="操作" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6" Margin="0,0,20,0"/>
            </Grid>

            <ListView Grid.Row="1" ItemsSource="{Binding Services}" SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,8"/>
                        <Setter Property="CornerRadius" Value="8"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="16" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                <StackPanel Orientation="Horizontal" Spacing="4" Margin="0,2,0,0">
                                    <Border Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" CornerRadius="4" Padding="4,0">
                                        <TextBlock Text="{Binding Id}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.5" FontSize="10"/>
                                    </Border>
                                    <TextBlock Text="{Binding Pid}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.5"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Ellipse Width="8" Height="8" Fill="{Binding Status, Converter={StaticResource StatusColorConverter}}"/>
                                <TextBlock Text="{Binding Status}" Style="{StaticResource BodyTextBlockStyle}"/>
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="{Binding ExePath}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding ExePath}" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>

                            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="4">
                                <Button Click="OnStartClick" Tag="{Binding Id}" ToolTipService.ToolTip="启动" CornerRadius="4">
                                    <FontIcon Glyph="&#xE768;" FontSize="12"/>
                                </Button>
                                <Button Click="OnStopClick" Tag="{Binding Id}" ToolTipService.ToolTip="停止" CornerRadius="4">
                                    <FontIcon Glyph="&#xE71A;" FontSize="12"/>
                                </Button>
                                <Button Click="OnLogsClick" Tag="{Binding Id}" ToolTipService.ToolTip="日志" CornerRadius="4">
                                    <FontIcon Glyph="&#xE9F9;" FontSize="12"/>
                                </Button>
                                <Button Click="OnEditClick" Tag="{Binding Id}" ToolTipService.ToolTip="编辑" CornerRadius="4">
                                    <FontIcon Glyph="&#xE70F;" FontSize="12"/>
                                </Button>
                                <Button Click="OnDeleteClick" Tag="{Binding Id}" ToolTipService.ToolTip="删除" CornerRadius="4" Background="Transparent" BorderThickness="0">
                                    <FontIcon Glyph="&#xE74D;" FontSize="12" Foreground="#E05050"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid Grid.Row="3" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16,6" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
        </Grid>
    </Grid>
</Window>
